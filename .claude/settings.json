{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[提醒] 提交前请确认: 1.遇到的问题已记录到docs/DEV_NOTES.md 2.任务进度已更新到docs/TASK_LIST.md'"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'INPUT=$(cat); COMMAND=$(echo \"$INPUT\" | jq -r \".tool_input.command // empty\"); if echo \"$COMMAND\" | grep -q \"git commit\"; then echo \"[提醒] 提交完成! 请检查 docs/DEV_NOTES.md 是否已记录本次遇到的问题和解决方案\"; fi'"
          }
        ]
      }
    ]
  }
}
